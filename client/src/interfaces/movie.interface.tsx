export interface MovieData {
    id: string | number;  // Added the id property for uniqueness
    title: string;
    actors: string;
    genre: string;
    description: string;
    releaseDate: string;
    poster: string;
  }
  